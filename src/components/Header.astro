---
const currentPath = Astro.url.pathname;

const navItems = [
  { href: '/', label: 'Baneos', icon: 'ban' },
  { href: '/mutes', label: 'Silencios', icon: 'mute' },
  { href: '/kicks', label: 'Expulsiones', icon: 'kick' },
  { href: '/warnings', label: 'Advertencias', icon: 'warning' },
];

function isActive(href: string): boolean {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---

<header class="sticky top-0 z-50 glass">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20 sm:h-24">
      <!-- Logo -->
      <a href="/" class="flex items-center gap-3 group">
        <div class="flex-shrink-0">
          <img 
            src="/images/logo.webp" 
            alt="Logo Mycraft Network" 
            class="w-16 h-16 sm:w-20 sm:h-20 object-contain transition-transform group-hover:scale-105"
          />
        </div>
        <div class="hidden sm:block">
          <span class="font-clash font-[550] text-xl text-white">Mycraft Network</span>
          <p class="text-sm text-zinc-400">IP: server.mycraft.es</p>
        </div>
      </a>

      <nav class="hidden md:flex items-center gap-1 bg-zinc-900/50 rounded-xl p-1">
        {navItems.map((item) => (
          <a
            href={item.href}
            class:list={[
              "relative px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200",
              isActive(item.href)
                ? "text-white bg-zinc-800"
                : "text-zinc-400 hover:text-white hover:bg-zinc-800/50"
            ]}
          >
            {isActive(item.href) && (
              <span class="absolute inset-x-0 -bottom-px h-0.5 bg-gradient-to-r from-purple-600 to-blue-600 rounded-full"></span>
            )}
            {item.label}
          </a>
        ))}
      </nav>

      <div class="flex items-center gap-4">
        
        <form action="/search" method="GET" class="relative hidden sm:block">
          <input
            type="text"
            name="q"
            placeholder="Buscar jugador..."
            class="w-48 lg:w-64 px-4 py-2 pl-10 bg-zinc-900/50 border border-zinc-800 rounded-xl text-sm text-white placeholder-zinc-500 focus:outline-none focus:border-purple-500/50 focus:ring-2 focus:ring-purple-500/20 transition-all"
          />
          <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-zinc-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </form>

        <button 
          type="button" 
          class="md:hidden p-2 rounded-lg text-zinc-400 hover:text-white hover:bg-zinc-800/50 transition-colors"
          id="mobile-menu-button"
          aria-label="Abrir menú"
        >
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div class="md:hidden hidden" id="mobile-menu">
    <div class="px-4 py-4 space-y-2 border-t border-zinc-800">
      <!-- Mobile Search -->
      <form action="/search" method="GET" class="relative mb-4">
        <input
          type="text"
          name="q"
          placeholder="Buscar jugador..."
          class="w-full px-4 py-3 pl-10 bg-zinc-900/50 border border-zinc-800 rounded-xl text-sm text-white placeholder-zinc-500 focus:outline-none focus:border-purple-500/50"
        />
        <svg class="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-zinc-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </form>

      {navItems.map((item) => (
        <a
          href={item.href}
          class:list={[
            "block px-4 py-3 rounded-xl text-sm font-medium transition-colors",
            isActive(item.href)
              ? "text-white bg-zinc-800"
              : "text-zinc-400 hover:text-white hover:bg-zinc-800/50"
          ]}
        >
          {item.label}
        </a>
      ))}
    </div>
  </div>
</header>

<script>
  const menuButton = document.getElementById('mobile-menu-button');
  const mobileMenu = document.getElementById('mobile-menu');

  menuButton?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>
